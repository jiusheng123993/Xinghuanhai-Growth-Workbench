# 审查报告（审查船员 审-1）· TASK-REVIEW-TABBAR-B3 自定义 tabBar

- 审查对象：`E:\星河宠记` HEAD `3b98952` 工作区（未提交改动），小程序工程根 `03-源代码/小程序/miniapp`
- 审查方式：只读。未修改任何仓库文件（`git` 仅用读命令；未跑 `git write`、未跑 `build:weapp`）
- 报告落盘位置说明：派活指令未给出路径，本沙箱只允许写工作区内，故落在仓库既有审查目录
  `01-产品文档/审查报告/`（与 2026-09-08 全项目双 Agent 审查报告同目录）

## 结论摘要

**改造方向成立、可交付：三条选中态路径（冷启动首页 / 首次进新 tab / 切回旧 tab）都不会错位，
16 张图标删除安全，4 个 tab 页的底部让位逐页复查无遗漏，`z-index: 900` 这个档位选得对；
未发现 P0。** 但发现 1 条「注释声称生效、微信端实际静默失效」的动效（P1，仅视觉），
以及 8 条注释/测试/口径层面的问题（P2），另有 6 项必须真机或开发者工具才能验证的事项。

**P0 = 0 · P1 = 1 · P2 = 8**

---

## 八个必答问题

### 1. 选中态机制是否自洽 —— 结论：三条路径都不错位，但「乐观更新」那条注释是错的

**①冷启动进首页：正确。** 证据链（全部可复现）：
- Taro 的页面配置在 `onLoad` 里**先**写入路由再渲染：`setCurrentRouter(this)`（`node_modules/@tarojs/runtime/dist/runtime.esm.js:4021-4034`，调用点 `:4053`）
  之后才 `Current.app.mount(...)`（`:4058-4071`）。
- `Taro.getCurrentInstance()` 返回的就是这个全局 `Current`（`runtime.esm.js:3568-3573`），
  且 `router.path` 经 `addLeadingSlash`（`runtime.esm.js:3941`）**带**前导斜杠。
- 组件初值 `resolveInitialSelected()`（`src/custom-tab-bar/index.tsx:62-71`）读到 `/pages/index/index` → 0 ✔
- 即便 tabBar 实例挂在页面 `onShow` 之后（页面 onShow 的组件广播是 `_raf` 异步派发的，`runtime.esm.js:4111-4124`），
  初值兜底仍然正确 —— 这正是文件头 `:21-30` 描述的分工。

**②第一次点进某个从未进过的 tab：正确。** 新页 `onLoad` 先设 router 再渲染树 → 该页 tabBar 组件
`attached` 时 `Current.router` 已是本页；即使页面 `useDidShow` 广播早于组件订阅（useEffect 是异步 flush），
初值也已经是本页下标。**置信度：高度可能**（推理基于 runtime 源码顺序，无真机复核）。

**③切回一个已存在的 tab：最终正确，但有一帧窗口。** 实例不随 `switchTab` 重建（微信官方口径：
每个 tab 页各一个 tabBar 实例），所以目标页实例被切回来时**先渲染上一次广播留下的值**（= 用户刚离开的那页下标），
要等「本页 onShow → `_raf` → 事件 → setState → setData」才纠正。
- 机制层面：**确定**（`runtime.esm.js:4123` 的 `_raf` + `src/custom-tab-bar/index.tsx:126-136` 的订阅）。
- 肉眼是否可见（一帧闪错高亮）：**不确定**，必须真机看。
- ⚠️ `handleTabClick` 里的「先 setSelected 再跳转」（`index.tsx:166-178`）改的是**即将被隐藏的那个实例**的本地 state，
  跨页切换后用户看到的是目标页实例，所以注释宣称的「防闪烁」在这条路径上**不成立**（见 P2-4）。

**其它路径补充**：4 个 tab 页都在组件函数体顶层无条件调用了 `useTabBarSelected`（`pages/index/index.tsx:95`、
`pages/timeline/index.tsx:430`、`pages/creative/index.tsx:168`、`pages/mine/index.tsx:141`），
hook 内部只做 `useDidShow` 广播，未登录/无宠物等分支都不会跳过它 ✔；非 tab 页（宠物档案等）推入时微信不渲染 tabBar ✔。

### 2. 底部让位是否完整 —— 结论：逐页复查**没有**被底栏压住的元素

方法：对 4 个 tab 页 scss + `src/components/**` 全量取 `position: fixed|sticky|absolute`，
再用脚本把每个命中定位回它最近的选择器（避免把卡片内装饰误判成页级贴底）。

| 页面 | 页级贴底/吸底元素 | 结论 |
| --- | --- | --- |
| 今天 | `.chat-input-area`（sticky，已抬到 `140rpx+safe`，`pages/index/index.scss:617-631`）；`.chat-plus-overlay`（fixed 全屏遮罩，`:926-935`）；`.naming-popup-overlay`/`.session-drawer-overlay`（fixed 全屏弹窗，`:1555`/`:2137`） | 输入区已让位；其余是遮罩/弹窗（见 Q3，均在底栏之上）✔ |
| 今天 | `.chat-plus-panel`（`position:absolute;bottom:110rpx`，`:938-950`） | **不是视口贴底**：它是 `.chat-input-area`（sticky 即定位包含块）的子节点，相对输入区贴底，天然在底栏之上 ✔ |
| 今天 | 骨架屏 `.home-skeleton` | 已同步减高（`components/HomeSkeleton.scss:37-44`），且全局 `view{box-sizing:border-box}`（`app.scss:27-33`）让 padding 含在内，不会溢到底栏后 ✔ |
| 时光 | `.timeline-review-overlay`（fixed 全屏、z1000，回忆详情弹窗） | 在底栏之上，属正常弹层；页内无其它贴底元素 ✔；根容器 `height:calc(100vh-160rpx-safe)`（`:29-30`） |
| 创作 | 4 处 absolute（`:214/:227/:245/:394`）全在 `.cve-feat`（relative）卡片内做插画/文字贴底 | 无页级贴底元素；根 `padding-bottom:160rpx+safe`（`:36-38`）✔ |
| 我的 | 全页 `position:fixed|sticky` **零命中**（脚本扫描） | `page{padding-bottom:160rpx+safe}`（`mine/index.scss:667-670`）作用在页面滚动容器上：`.mine-page` 只有 `min-height:100vh`（`:29-30`）、`<ScrollView scrollY>` 未给 height（`mine/index.tsx:269`）→ 整页滚动、让位生效 ✔ |

数值口径自洽：100rpx（底栏板高，`custom-tab-bar/index.scss:35`）+ 40rpx（圆钮上凸，`:76` 的 `margin-top:-40rpx`）+ 20rpx（呼吸位）= 160rpx，
与 index/timeline/creative/mine 四页各自写法一致 ✔。唯一例外是 index 输入区的 `140rpx`（=100+40，无呼吸位）→ 见 P2-6。

### 3. z-index 是否选对 —— 结论：900 选对了，全仓无「页面内容压底栏」也无「合法遮罩被底栏压住」的回归

全仓 `z-index` 扫描（`rg -n "z-index" src -g '*.scss'`）后的完整分层：

| 档位 | 代表元素 | 是否在 tab 页 |
| --- | --- | --- |
| -1 | `.xhh-bg`（PageBackground 背景层） | 全站 |
| 0 / 1 / 2 / 5 / 9 / 10 / 20 | 页内装饰、卡片内部层、`AnomalyMarker`、`timeline` hero、`family` 卡片 | — |
| 10 | `app.scss:131` 给 `.chat-home-page/.mine-page/.timeline-page/...` 的内容层 | 4 个 tab 页都吃这条 |
| 50 | `index` 顶部吸顶栏 `.chat-top-bar` | 今天 |
| **99** | `index` `.chat-plus-overlay`（**全屏 dim 遮罩**）、`.chat-input-area`；`family-tree:665` | 今天 |
| 100 | `index` `.chat-plus-panel`；`memoir-*`/`breed`/`food-query`/`family-tree` 的贴底条与面板 | 今天（其余非 tab 页） |
| 200 / 300 | `breed` 面板、`share-card` 工具条 | 非 tab 页 |
| **900** | **本轮新增：`.custom-tab-bar`**（`custom-tab-bar/index.scss:28`，dist 产物同值） | 4 个 tab 页 |
| 999 | `HealthTrendShareCard`/`VaccineShareCard`/`NpsSurvey` 的 overlay、`symptom-check:461` | 非 tab 页 |
| 1000 | 弹窗/抽屉：`PrivacyPopup`、`PetDeceasedModal`、`CarePlanCard`、`AnxietyIntervention`、`index` 的命名弹层与抽屉、`timeline` 回忆评审遮罩、`settings:154`… | 含 tab 页（均应为弹层） |
| 1050 / 2000 / 9999 | `CheckinPopup`、`SymptomCheckPopup` / `EmergencyAlert` / `LogoLoading` | 可能出现在 tab 页 |

**①有没有落在 0..899 的全屏遮罩/贴底弹层会被底栏压住？** 只有一处：`pages/index` 的 `.chat-plus-overlay`（99，全屏 dim）。
但它**不是回归**：改造前原生 tabBar 在 webview 之外、同样永远在页面遮罩之上且可点，
现在的表现（底栏不被 dim、点底栏可切页）与原行为一致；`plusPanel` 本身已证明在底栏之上（Q2）。
→ 不改代码，但建议在 `custom-tab-bar/index.scss:18-27` 那条分层说明里补一句
「页面内任何 z<900 的全屏遮罩都会被底栏压住，这是既有行为，不是 bug」。
**②有没有页面内容层级高于 900 把底栏盖掉？** 没有：>900 的全部是遮罩/弹窗/分享卡，无一是普通内容 ✔

### 4. 事件订阅是否泄漏 / 有无副作用 —— 结论：配对严格、负载一致、无串扰；但单测证明力不足

- **on/off 严格配对**：`index.tsx:117-124`（themeChange）与 `:127-136`（tabBarSelect）都用「同一函数引用」注册与注销；
  `Taro.eventCenter` 是 `@tarojs/shared` 的 `Events` 实例（`runtime.esm.js:3429`），其 `off(name, cb)` 只摘掉 `cb !== callback` 以外的回调并重新挂回其余的
  （`@tarojs/shared/dist/shared.esm.js:405-436`）→ 不会误删别人的监听 ✔ **确定**
- **负载类型一致**：生产端 `Taro.eventCenter.trigger('themeChange', theme: ThemeKey)`（`stores/themeStore.ts:333`）；
  消费端 `(next: ThemeKey) => setTheme(next)`（`custom-tab-bar/index.tsx:118`）与 `useThemeClass` 的 `(t: ThemeKey)`（`hooks/useThemeClass.ts:32`）一致 ✔
- **无串扰**：`tabBarSelect` 全仓只有 `constants/tabBar.ts:70` 定义、`:98` 触发、组件 `:132` 订阅；`themeChange` 的其它消费者（useThemeClass、Illustration、breed/breed-detail）都在刷新本地 state，无副作用 ✔
- **泄漏面**：4 个 tab 页各 1 个 tabBar 实例、每个实例 2 个监听，实例不重建 → 常驻 8 个监听，无累积 ✔
- ⚠️ 但**测试证明力不足**：单测的 eventCenter mock 是「事件名 → 单个回调」的 map，`off(name)` 直接 `delete`（`custom-tab-bar/__tests__/index.test.tsx:21-36`），
  因此「重复注册」「漏解绑后仍收事件」这两类事故它**测不出来**（见 P2-5）。

### 5. 配置是否真的单一真相源 —— 结论：两份数据现在完全一致；「靠单测对齐」够用但不完整

逐项核对（`src/constants/tabBar.ts:47-52` ↔ `src/app.config.ts:37-62`）：

| # | TAB_BAR_TABS.pagePath / text / icon | app.config pagePath / text / iconPath |
| --- | --- | --- |
| 0 | `/pages/index/index` 今天 home | `pages/index/index` 今天 `assets/icons/home.png` |
| 1 | `/pages/timeline/index` 时光 timeline | `pages/timeline/index` 时光 `assets/icons/timeline.png` |
| 2 | `/pages/creative/index` 创作 creative | `pages/creative/index` 创作 `assets/icons/creative.png` |
| 3 | `/pages/mine/index` 我的 mine | `pages/mine/index` 我的 `assets/icons/mine.png` |

路由顺序、文案、条数（各 4）全部一致 ✔；`appConfig.pages` 共 6 个主包页面，除这 4 个外只有
`pet-profile`/`family`，**没有别的 tab 页混进来** ✔；`tabBar.list` 无第三方消费者
（全仓只有 app.config 自身 + 单测 + Taro 编译；`rg "tabBar\.list"` 无其它命中）✔。

够不够稳：
- 单测钉住了 pagePath / text / 条数 / icon 名（`constants/__tests__/tabBar.test.ts:21-35`、`:70-78`），能挡住最常见的漂移 ✔
- **没钉** `app.config` 的 `iconPath` 与 `tab.icon` 的对应关系。今天若有人把 constants 里 `icon: 'home'` 改成 `'home2'`，
  app.json 仍写 `home.png`，测试全绿（运行时走 constants，所以只是 app.json 描述失真，影响小）→ P2-8
- 单测靠 `defineAppConfig` 透传 + 动态 import（同文件 `:13-18`）加载 app.config，
  因此 app.config 里**不能出现 import**，否则这条链路会变脆（目前无 import ✔，保持现状即可）
- 真要想「一份数据」：可以让 app.config 直接 import `TAB_BAR_TABS` 生成 list，但需先确认 Taro 的 config 编译支持 import；
  风险高于收益，建议先用「加一条 iconPath 断言」的最小改法

### 6. 删除 16 张图标是否真的安全 —— 结论：**安全**；但「历史死资源」这个说法只对了一半

**(a) 全后缀检索零引用（我独立重跑，不看施工方结论）**
范围：`src/**`、`miniapp/config/**`、`miniapp/scripts/**`、miniapp 根配置文件，后缀 `.ts/.tsx/.js/.jsx/.json/.scss/.css/.wxml/.wxss`，
排除 `node_modules`/`dist`/`.work-tmp`/`android`，共 655 个文件。用 Node 脚本按正则扫
`assets/icons`、`pet(-active)?.png`、`chat(-active)?.png`、`family(-active)?.png`、`member(-active)?.png`、`icons/... + 变量` 拼接：
- 16 张图标：**零引用** ✔
- 唯一命中 `pet.png` 的 3 处是测试夹具 `wxfile://tmp/pet.png`（壁纸路径，与图标无关）
- `assets/icons` 的命中只有：app.config 的 4 条 iconPath、themeStore 的 5 套主题目录字符串（`:117/:140/:163/:187`）、`getTabBarIconDir`（`:286`）、`config/index.js:48` 的整目录 copy 规则、注释与测试期望值

**(b) 运行时字符串拼接路径**：全仓唯一会拼 tabBar 图标路径的是 `custom-tab-bar/index.tsx:245`
`` `${iconDir}/${tab.icon}${active ? '-active' : ''}.png` ``；`iconDir` 来自 `getTabBarIconDir()`（5 套：`assets/icons` + `tabbar/{spring,summer,winter,starry}`），
`tab.icon ∈ {home, timeline, creative, mine}` → **再也拼不出 pet/chat/family/member** ✔

**(c) `Icon.tsx` / `icons-fill.ts` 的联合类型怎么区分**：那是**内联 SVG 图标名**（`FillIconName`，如 `paw-print`/`trophy`/`syringe`），
渲染走 `<Icon name=...>` → SVG path（不读 PNG）。三条区分依据：①该类型所有使用处都是 `<Icon name={...}>`；
②`src/assets/icons/` 下不存在同名文件（没有 `paw-print.png`）；③dist 里这些名字只出现在 JS chunk 与 wxss，不在 assets 目录。

**(d) 历史是否进过 dist（我独立查了 git 历史）**
- `git ls-tree -r 9161708 -- .../dist/assets/icons` → `{chat, chat-active, family, family-active, home, home-active, mine, mine-active, pet, pet-active}`
  → **chat/family 曾经在提交里的 dist 中存在过**（更早的「聊天/家庭」4-tab 时代 app.config 引用过它们，
  可见 4a6c2b9 的 `.board/diffs/*.diff` 里 `src/app.config.ts` 与 `dist/app.json` 的同名内容）
- `git log --all --diff-filter=A -- .../dist/assets/icons/{chat,family,member}.png` → 命中 `9161708`/`0576337`
- 文件真实大小（`git cat-file -s HEAD:<path>`）：`chat.png`/`chat-active.png` 各 **70 B**、`member(-active)` 127/123 B（**空白占位图**）、
  `family(-active)` 1193/1184 B、`pet(-active)` 3620/2348 B
→ 所以「历史死资源」应更正为「**随 IA 演进逐批退役**」。**结论不变：当前删除安全**（源码与 dist 双零引用，
dist 里 `rg "assets/icons/pet"` 0 命中），16 张合计释放 **32 059 B ≈ 31.3 KB**（其中 chat/family/member 6 张仅 2767 B ≈ 2.7 KB，与 09-11 那次记录的 2.7KB 完全吻合）。
当前 `dist/assets/icons` 恰为 40 张（根 8 + spring/summer/winter/starry 各 8）✔

### 7. 功能可达性 —— 结论：入口够用（有 1 条无条件常驻入口），但 app.config 里那条「首页宠物卡」入口**不存在**

`rg "pet-profile/index" src -g '!**/__tests__/**'` 的全部命中（逐条判断可达条件）：

| # | 位置 | 跳法 | 可达条件 |
| --- | --- | --- | --- |
| 1 | `pages/mine/index.tsx:88`（「数据服务」组新增的「宠物档案」行，渲染逻辑 `:430-453` 无条件） | `navigateTo` | **无条件常驻**（不依赖登录态/宠物数；未登录进页后由页面守卫接管）→ 最稳 |
| 2 | `pages/family/index.tsx:346-356` | `switchPet → navigateTo` | 已登录 + 有家庭 + 家庭内有宠物；切换失败会 toast 且**不跳**（有兜底） |
| 3 | `pagesPet/achievement/index.tsx:138-148`（空态按钮「去打卡」） | `navigateTo` | **只在「还没有成就」时出现**；有成就则渲染卡片列表，该按钮不存在 |
| 4 | `src/app.config.ts:6` 注释声称的「首页宠物卡」 | — | **不存在**：`rg "pet-profile" src/pages/index` 0 命中；首页全部 navigateTo 目标（`index.tsx:275/287/366/367/591/615/655/918/925/932/945`）里没有它，`.home-hero`（`:625-651`）也没有 onClick |

- 会不会「某个用户路径下再也进不去」？**不会**：入口 1 与登录态/宠物数无关，覆盖全部用户路径。
- 但**最短路径变长了**：改前 tab 一点即到，改后要「我的 → 宠物档案」两步；首页直达档这条路径确实断了。
  若这是有意的 IA 取舍，只需把 app.config 注释改对（P2-1）；若想保留首页直达，得给首页宠物卡补一个 onClick。
- 顺带核查「还有没有别的页在 switchTab 到宠物档案」（会静默失败的那种）：
  `rg "switchTab" src` 的全部调用点中，只有 `pages/family`（已改 navigateTo）、`pagesPet/achievement`（已改）、
  `pet-profile → timeline`（timeline 是 tab 页，正确）、`creative → index`、`platform/ui`、`utils/navigation`（fallbackUrl 默认首页，且全仓无覆盖）→ **没有残留的错误 switchTab** ✔

### 8. `aria-label` 到底生不生效 —— 结论：**微信端确定无效**（H5 端有效）；建议保留但把注释改成「确定」

- Taro 类型：`node_modules/@tarojs/components/types/View.d.ts:52-58` 只有 `ariaRole?: string` / `ariaLabel?: string`，
  两处注释都标 `@supported qq`，**没有** kebab 形式的 `aria-label` → 直接写会 TS 报错，这解释了 `ariaLabelProps()`（`custom-tab-bar/index.tsx:92-105`）的存在。
- 编译产物（决定性证据）：`dist/base.wxml` 的 view 模板属性是**闭集** ——
  `hover-class / hover-stop-propagation / hover-start-time / hover-stay-time / animation / bindtouch* / style / class / id / data-sid`，
  没有任何通用属性透传；`rg -c "aria-label" dist -g '*.wxml'` → **0 行**；全 dist 只有 `dist/custom-tab-bar/index.js` 里那一个字面量（props 对象，模板不读）。
- 顺带同源发现：`data-active` 也**不会落进 wxml**（`rg -o "data-[a-z-]+=" dist/base.wxml` 只输出 `data-sid=`；
  image 模板只有 `src/mode/lazy-load/webp/show-menu-by-longpress/style/class/id/data-sid` + `i.p*` 槽位）→ 见 P1-1。
- 微信侧无障碍属性是 wxml 上的 `aria-label`/`aria-role`，Taro 不产出 ⇒ **微信端确定无效**（不是「不确定」）。
- 判断：**保留**。理由：H5 端会真的落到 DOM（注释里这句话对 H5 成立）、weapp 端零成本零副作用、删掉要连带动单测（`__tests__/index.test.tsx:119-125`）。
  但注释里「微信端的行为见自述『不确定项』」应改成「微信端确定无效（Taro 的 weapp 模板不产出该属性）」。
  真要在微信端做无障碍只能自定义 wxml 模板，成本远大于收益。

---

## 审查维度小结

- **代码质量**：中文注释密度与「写为什么」的纪律保持得很好；命名与结构清晰；两处 `useEffect` 分工写得准确。
  问题集中在「注释描述的行为已不成立」（P1-1、P2-1~4、P2-7）与「单测够不到真机机制」（P2-4、P2-5）。
- **逻辑正确性**：三条选中态路径、未登录/无宠物/单宠物/深色主题等分支都不会让底栏错位或消失；
  主题在 store 初始化时已是同步读本地存储（`themeStore.ts:312` 的 `current: getStoredTheme()`），
  `loadTheme()`（`:316-320`）不触发 `themeChange` 也不会让底栏画错主题（组件挂载时直接读 store 现值）✔。
  唯一数值口径瑕疵是 index 的 140 vs 160（P2-6）。
- **安全风险**：本轮改动**没有**引入新的越权/注入/敏感信息面：底栏只读 `ThemeMeta` 与常量，不发请求、不写日志、
  不接触 token；新代码里没有 `console.log`；`ai-manager.png` 是既有本地资产（未新增资源）✔
- **架构合规**：中文注释 ✔；颜色全部走主题（`barStyle`/`aiStyle` 由 `ThemeMeta` 注入，scss 里没有
  `rgba(var(--x), α)` 这种小程序非法写法，组件 scss 顶部还专门写了这条坑）✔；不新增主包资源 ✔
  （整目录 copy 规则只是让默认套图标不再依赖 app.config 的隐式链路，拷贝清单从改前的 42 张变成 40 张，**净减 2 张**）。
  偏差一处：`constants/tabBar.ts` 同时导出数据与 React hook，成为 constants 目录里**唯一**引入 `@tarojs` 的文件（P2-8）。
  另注：任务书提到的「七层边界」在本仓库已被 2026-09-08 审查判定为 PRD 虚构内容（`01-产品文档/PRD.md:10`），
  故我按实际目录约定（constants/services/stores/hooks/components/pages 分层 + 单一真相源）评估，未按七层结构挑毛病。

---

## P0（必须改，否则不能交付）

无。功能、路由、体积、图标、让位、层叠六项均未发现阻断性缺陷。

## P1（应该改）

### P1-1 选中弹跳动效在微信端静默失效（注释却写「这里就是它的使用者」）

- **现象**：`custom-tab-bar/index.tsx:242-247` 给 `<Image>` 挂 `xhh-tab-bounce` + `data-active`，
  期望「选中时弹一下」。在微信端这两个东西都不会生效，动效永远不会播。
- **证据（两条独立成因，第一条是产物级硬证据）**：
  1. `data-*` 属性不会被 Taro 的 weapp 模板渲染：`rg -o "data-[a-z-]+=" 03-源代码/小程序/miniapp/dist/base.wxml`
     输出**只有** `data-sid=`；image 模板属性闭集为 `src/mode/lazy-load/webp/show-menu-by-longpress/style/class/id/data-sid`（+`i.p*` 槽位）
     ⇒ 属性选择器 `[data-active='true']` 在真机上**永远匹配不到**。
  2. 该动效类定义在全局 `app.scss:166-169`，而微信规则是「**除继承样式外，app.wxss 中的样式对自定义组件无效**（标签名选择器等少数情况例外）」；
     本组件是项目里**唯一**走 `createComponentConfig` 的独立组件（`src/custom-tab-bar/index.config.ts` 是 src 下唯一非页面的 index.config.ts），
     Taro 只为内部 recursive 组件补 `addGlobalClass: true`（`runtime.esm.js:4267-4283`），
     用户组件只透传 `component.options`（`:4195-4236`，本组件没有 options）→ 默认 `isolated`。
     产物侧一致：`rg -c "xhh-tab-bounce" dist/custom-tab-bar/index.wxss` 无命中，该字符串只出现在 `dist/app-origin.wxss`。
- **影响**：纯视觉 —— 选中态仍靠换图（`-active.png`）表达，用户不会「用不了」，但少了一个设计里承诺的反馈。
  另外这条注释会误导后人以为全局动效类「已经被用上了」。
- **建议改法（二选一，建议①）**：①把弹跳改成组件内类名 + 组件自身 scss 定义
  （`className={active ? 'custom-tab-bar__icon custom-tab-bar__icon--active' : 'custom-tab-bar__icon'}`，
  把 `@keyframes xhh-tab-pop` 与 `prefers-reduced-motion` 降级一起搬进 `src/custom-tab-bar/index.scss`），
  与已经写在组件内的 `.custom-tab-bar__ai--press`（`:88-91`）保持同一种做法；
  ②或在 `src/custom-tab-bar/index.config.ts` 里加 `styleIsolation: 'apply-shared'`，但**只解决原因 2，解决不了 `data-active` 不落地**，仍需改选择器。
- **置信度**：原因 1 → **确定**（产物证据）；原因 2 → **高度可能**（微信文档 + 运行时源码 + 产物，未真机 A/B）。
  若真机实测弹跳有效（即我对样式隔离判断有误），本条降级为 P2（仅需修注释 + `data-active` 仍是死属性）。

## P2（可以改）

### P2-1 `app.config.ts` 注释与事实不符：首页宠物卡入口不存在
- **现象**：`src/app.config.ts:6` 写「入口在『我的』页与首页宠物卡（navigateTo 进入）」。
- **证据**：`rg "pet-profile" src/pages/index` → 0 命中；首页所有 `navigateTo` 目标见 Q7 表格；`.home-hero`（`:625-651`）无 onClick。
- **影响**：误导后续 IA 决策（以为首页仍有一条直达档案的路径）。
- **建议**：删掉「与首页宠物卡」这半句，或补一个真实入口（首页宠物卡目前不可点）。
- **置信度**：确定。

### P2-2 `app.config.ts:11-14` 把 `pages/timeline/index` 归到「保留但退出 tabBar」
- **现象**：注释分组写「以下页面保留但退出 tabBar…不能从 tabBar 直接进入」，紧跟着的就是 `pages/timeline/index` 与 `pages/family/index`，
  而 timeline **就是本轮 tabBar 的第 2 项**（`app.config.ts:44-49`）。
- **证据**：`app.config.ts:11-14` vs `:37-62`；`constants/tabBar.ts:49`。
- **影响**：这是本次要重点排查的「残留错误陈述」——照注释办事的后人会对着 tab 页写 `navigateTo`（微信端静默失败，不报错、页面不动）。
- **建议**：把这段注释改成只描述 `family`（并注明 timeline 已于 IA 第 3 批回到 tabBar）。
- **置信度**：确定。

### P2-3 两处过期注释（本轮把原生标签栏链路删掉后未同步）
- **证据**：`stores/themeStore.ts:71` 仍写「// 标签栏（原生组件，需 Taro.setTabBarStyle 动态设置）」；
  `hooks/useThemeClass.ts:14` 仍写「每个页面挂载/显示时自动调用 applyNativeBars 确保导航栏**和标签栏**颜色正确」——
  两处都在描述本轮已删除的能力（`applyNativeBars` 现在只剩导航栏，`themeStore.ts:290-309`；全仓已无 `setTabBarStyle` 调用）。
- **影响**：后人会以为底栏配色还有一条「原生 API 通道」。
- **建议**：分别改为「自定义组件按主题自绘」（并把 `tabBarBg/Color/SelectedColor/BorderStyle` 的属主写清是 `custom-tab-bar`）。
- **置信度**：确定。

### P2-4 「乐观更新防闪烁」的注释与实现不符
- **现象**：`custom-tab-bar/index.tsx:166-174` 说「先 setSelected 再跳转…否则快速连点两个 tab 时会看到高亮闪一下」，
  但 `handleTabClick`（`:175-178`）改的是**当前页实例**的本地 state，切走后该实例不可见，
  目标页用的是它自己的实例 → 这条「乐观更新」在跨页路径上不产生任何可见效果（Q1 ③）。
- **影响**：注释把真正起作用的机制（目标页 `useDidShow` 广播 + 路由初值兜底）说成了次要机制，容易在后续重构里被误删。
- **建议**：要么删掉这句乐观更新（并注明「跨页切换时实例不同，乐观更新仅影响当前实例，实际由目标页广播纠正」），
  要么把选中态提升为全局（`eventCenter` 广播 + 组件只读），那才是真正的「消除一帧延迟」。
- **置信度**：机制确定；「那一帧是否肉眼可见」不确定。

### P2-5 事件 mock 保真度不足，测不出「重复注册 / 漏解绑」
- **证据**：`custom-tab-bar/__tests__/index.test.tsx:21-36` 的 `on(name, fn)` 只存一个回调、`off(name)` 直接 `delete`；
  因此断言里虽然写了「卸载时解绑」（`:202-209`），但即使有人把 `useEffect` 依赖写错导致重复注册，用例照样全绿。
- **建议**：mock 改成「事件名 → 回调数组」，补一条「重复注册后只收到一次 / 卸载后触发事件不应再 setState」的断言。
- **置信度**：确定。

### P2-6 数值口径 140 vs 160（sticky 兜底分支会与中心圆钮贴边）
- **证据**：底栏板 100rpx（`custom-tab-bar/index.scss:35`）+ 圆钮上凸 40rpx（`:76`）→ 圆钮顶边在视口底上方 140rpx；
  其它三页留 160rpx（含 20rpx 呼吸位），而 `pages/index/index.scss:630-631` 写 `calc(140rpx + safe)`。
- **影响**：当该 sticky 分支真的生效（页面整页滚动）时，输入区底边正好与圆钮顶边重合，无呼吸位（当前布局下该分支基本不可达，
  因为根容器是 `min-height` 而滚动发生在内部 `.chat-msg-list`）。
- **建议**：兜底值改 `150~160rpx`，或把注释里的口径算式改成 100+40（不带呼吸位）以消除读者困惑。
- **置信度**：算术确定；可达性判断为高度可能。

### P2-7 `aria-label` 注释应改成「确定无效」
- **证据**：见 Q8（`dist/base.wxml` 模板闭集、全 dist 仅组件 JS 出现该字符串）。
- **建议**：保留实现，注释从「微信端的行为见自述『不确定项』」改为「微信端确定无效（Taro weapp 模板不产出该属性），仅 H5 生效」。
- **置信度**：确定。

### P2-8 `constants/tabBar.ts` 混入 React hook 与平台依赖；单一真相源仍靠「两份数据 + 单测」
- **证据**：`constants/tabBar.ts:18` 是 constants 目录里**唯一**的 `@tarojs` import（其余 constants 均为纯数据），
  且该文件同时导出 `useTabBarSelected`（`:111-115`）这种页面级 hook；项目已有 `src/hooks/`（30+ 个 hook 文件）。
  另外单测未校验 `app.config` 的 `iconPath` 与 `tab.icon` 的对应（见 Q5）。
- **影响**：constants 从「纯数据」变成「数据 + 平台行为」，单测/复用需要 mock Taro；app.json 的图标描述可能与 constants 漂移而无告警。
- **建议**：hook 拆到 `src/hooks/useTabBarSelected.ts`（constants 只留数据 + 纯函数 `tabIndexByPath`）；
  单测补一条 `iconPath === `assets/icons/${tab.icon}.png`` 断言。
- **置信度**：确定。

---

## 我独立复算过的数字（命令 + 真实输出）

1. 类型闸门：`node node_modules/typescript/bin/tsc --noEmit` → **TSC_EXIT=0**（0 错）
2. 单测：`node node_modules/vitest/vitest.mjs run --reporter=dot` →
   `Test Files 169 passed | 1 skipped (170)`；`Tests 2889 passed | 44 skipped (2933)`；`Duration 22.55s`；`VITEST_EXIT=0`
   （即施工方的「2889 passed / 0 failed」成立，但**漏报了 44 个 skipped**）
3. 主包体积（自己按 dist 磁盘逐文件求和，不用施工方脚本）：
   `dist 总 4 065 074 B` − `pagesPet 1 524 389 + pagesMemoir 160 040 + pagesUser 582 000 = 2 266 429 B`
   → **主包 1 798 645 B = 1756.5 KB**（< 1900KB 验收线；微信按 IDE 编译后体积判定，≈磁盘 + 100KB）
4. dist 断言：`dist/custom-tab-bar/` 四件套存在（`index.js/json/wxml/wxss`，其中 `index.json` = `{"component":true}`）；
   `dist/app.json` 含 `"custom":true` 且 list 恰 4 项、顺序 今天/时光/创作/我的；
   `dist/assets/icons` 恰 **40** 张（根 8 + spring/summer/winter/starry 各 8）；`rg "assets/icons/pet" dist` **0 命中**
5. 产物对应当前源码：dist 构建时间 `2026-09-11T19:37:54Z`（= 北京时间 09-12 03:37）**晚于**本轮源码编辑时间
   （`custom-tab-bar/index.tsx` 19:34:24Z、`constants/tabBar.ts` 19:34:12Z、`pages/mine/index.scss` 19:36:02Z）
6. 删除图标的真实体量：16 张合计 **32 059 B ≈ 31.3 KB**（其中 chat/family/member 6 张仅 2767 B ≈ 2.7 KB，与 09-11 记录一致）
7. 历史证据：`git ls-tree -r 9161708 -- .../dist/assets/icons` = chat/family/home/mine/pet 及其 -active 共 10 项
8. 全仓 z-index 表、`position` 定位表、`pet-profile` 入口表均见上文（命令：`rg -n "z-index" src -g '*.scss'`；
   Node 脚本「命中行 → 最近选择器」；`rg -n "pet-profile/index" src -g '!**/__tests__/**'`）
9. 工作区范围核对：`git status --short -- 03-源代码/小程序/miniapp` = **35 项**（16 删除 + 13 修改 + 2 新增文件 + 1 新增目录），与任务书清单一致

## 我**没能**验证的东西（如实列）

1. **真机/开发者工具实跑**：必须用微信开发者工具或真机才能看的四项 ——
   ①切回旧 tab 的那一帧错高亮是否肉眼可见；②`env(safe-area-inset-bottom)` 在自定义 tabBar 下是否真的 >0（我所有让位算式都建立在
   「自定义 tabBar 后 webview 铺到屏幕底」这一条上）；③4 个 tab 页底部 60rpx 余量的观感；④底栏与页面遮罩在真机上的实际层叠
   （自定义组件宿主节点是否创建层叠上下文，我按「不创建」推理，未实测）。
2. **`xhh-tab-bounce` 的真机 A/B**：我只做到「产物 + 文档 + 运行时源码」级论证；`data-active` 不落地是产物级确定，
   「app.wxss 类选择器对自定义组件无效」是高度可能但未在真机验证。
3. **没有重跑 `build:weapp`**（按要求不做重活）：因此「dist 与当前源码一致」是用文件时间戳 + 产物内容抽样（140rpx/160rpx/custom-tab-bar 常量）证明的，
   不是重新编译比对。微信 CLI 的 `preview`/`upload`、IDE 侧体积与代码质量判定我也没跑。
4. **H5 端**（本项目当前不交付）：`aria-label` 落到 DOM、`xhh-tab-bounce` 在 H5 生效这两条我只有类型/代码层推理。
5. **`app.config` 由常量生成**这条「真正一份数据」的可行性：未验证 Taro 的 config 编译是否支持 import，故只作为备选建议提出。
6. **`page{padding-bottom}` 在「我的」页真实滚动到底时的截图**：只做了 CSS 推理（`.mine-page` 无固定高度 → 整页滚动 → page padding 生效）。

## 对施工方结论的纠正（他们说的哪一条不成立）

1. **「chat/family/member 是历史死资源」不准确**：它们在更早的「聊天 / 家庭 / 会员」tab 时代是**在用的** tabBar 图标，
   并且**确实进过提交里的 dist**（`git ls-tree -r 9161708`、`git log --all --diff-filter=A`）。
   现在的删除依然安全（源码 + 当前 dist 双零引用），但措辞应改为「随 IA 演进逐批退役」，
   否则后人会以为 `dist/` 历史里从没有过它们。附：chat/member 是 70B/127B 的空白占位图。
2. **「切 tab 时先 setSelected 可防高亮闪烁」这条设计意图不成立**：跨页切换用的是不同实例，乐观更新作用在被隐藏的实例上；
   真正保证正确的是「目标页 `useDidShow` 广播 + 冷启动/新实例的路由初值兜底」。
3. **「事件订阅严格配对」结论对，但证据链不足**：现有单测的 eventCenter mock 按事件名只存一个回调，
   证明不了「重复注册/漏解绑」，建议补强 mock 后再把它当作防回归闸门。
4. **闸门数字需补一句**：`2889 passed` 正确，但完整输出是 `2889 passed | 44 skipped`（169 文件 passed + 1 skipped）。
5. **`aria-label` 的「不确定项」其实可确定**：微信端无效（Taro weapp 模板不产出该属性），无需留作开放问题。
6. **注释清理未做干净**：本批新增注释里「组件实例」这条已改对 ✔，但仍有 4 处残留不成立的陈述
   （`app.config.ts:6`、`app.config.ts:11-14`、`stores/themeStore.ts:71`、`hooks/useThemeClass.ts:14`），
   另加 `index.tsx:236-241` 关于 `xhh-tab-bounce` 的「这里就是它的使用者」与事实不符（见 P1-1）。
