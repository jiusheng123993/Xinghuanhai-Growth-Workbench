
| 2026-09-10 00:10 | **回忆录提示词人机协同 + 画风 + BGM内置合帧 后端部署（用户「部署后做用户导入」授权）**：①迁移 036 memoir_prompt_confirmations 表建表成功（sudo -u postgres，psql 复查在位）；②8 个 src 文件上传（memoirService/memoirProcessor/memoirScriptService/videoGenerationService/memoirRepository/routes memoir/schemas index/rateLimit：提示词 preview/refine/confirm 三接口 + processor 确认版优先采用/持久化 + 画风 STYLE_PRESET 注入 + BGM_FILES 合成混音；备份 src.bak.promptbgm-20260909232048）；③BGM 4 资产（incompetech CC BY 3.0）上传 uploads/bgm/；④PM2 restart（115）online；⑤冒烟全绿：health 200、三提示词路由 401、error 0。⚠️ 前端（提示词确认/画风 chips）需微信开发者工具重新编译发版后可见 | ✅ 已部署；回滚=恢复 src.bak.promptbgm-20260909232048 重启 |

| 2026-09-10 01:35 | **回忆录 BGM 用户导入完整闭环部署（承接「部署后做用户导入」）**：①BGM 音频上传端点 POST /api/pets/:petId/memoir/bgm/upload（memoir.ts，multer memoryStorage+归属校验+mp3/m4a/aac/wav+12MB）；②customBgmUrl 全链路：下单(product_metadata)→回调 createMemoirFromPayment(narrative.custom_bgm_url)→processor→generateMemoirVideo(customBgmUrl)→stitch 优先下载用户音频循环混音（替代内置 incompetech 曲）；③备份 7 文件 /opt/xinghuanhai/src.bak.bgmimport-202609100130；④PM2 restart（116）online；⑤冒烟全绿：health 200、bgm/upload 401（加载+鉴权）、error 0。⚠️ 前端「导入我的音乐」入口/选中/下单（551c680）需微信开发者工具重新编译发版后可见 | ✅ 已部署；回滚=恢复 src.bak.bgmimport-202609100130 重启 |

| 2026-09-10 04:00 | **聊天「发图片」真·上传+AI看图分析 后端部署（用户授权）**：①新增 POST /api/ai/photo-analyze（routes/ai.ts：authMiddleware+uploadLimiter+upload.single，复用 visionService.analyzeImage，图→dataURL→结构化照片观察描述，只描述可见内容不确诊不编造，不落库；未传图400/视觉key未配置503/异常500统一文案）；②共享 multer 加 MIME 白名单 fileFilter（image/jpeg,png,webp,heic+audio/mpeg,mp3,mp4,wav,aac,m4a——一处修复覆盖 /photo-analyze、/breed-recognize、/health-report-recognize、/voice）；③备份 1 文件 /opt/xinghuanhai/server/src.bak.photoanalyze-20260910035958/routes/ai.ts；④PM2 restart（117）online；⑤冒烟全绿：health 200、photo-analyze 未登录 401（路由已注册）、error 0（唯一既有 warning=旧 AI_API_KEY 语义迁移建议，非本次引入）。⚠️ 前端（useChatCore/ChatContext.imageAnalysis）需微信开发者工具重新编译发版后可见；前端改动已提交 ecc02a1（develop） | ✅ 已部署；回滚=恢复 /opt/xinghuanhai/server/src.bak.photoanalyze-20260910035958/routes/ai.ts 重启 |
| 2026-09-10 04:20 | **食物查询数据源修正确保上线（西瓜=safe 修复）部署（用户授权）**：①原因=独立食物查询页/聊天食物卡片走 /api/food/query 只查 pet_food_queries(占位表)而非权威表 pet_food_safety_knowledge，导致西瓜这类"权威库=safe"食物在查询页回 caution(注意)，且接口返回数组而前端 queryFood 期待单条对象、契约不一致；②改动 3 文件（repositories/foodRepository.ts 新增 searchKnowledge 优先查权威表 + routes/food.ts 优先=权威库→用户历史→占位、返回单条对象、'danger'→'dangerous' 归一 + services/agentService.ts 系统提示词强化如实转述 food 结论）；③备份 /opt/xinghuanhai/server/src.bak.foodfix-20260910041755/{repositories,routes,services}；④上传 3 文件（scp，md5 校验一致）；⑤PM2 restart（118）online；⑥冒烟全绿：/api/health 200、/api/food/query 未登录 401（路由已注册）、err.log 无本次相关 error（既有 voice ASR 400 属语音输入旧问题、非本次）。⚠️ 前端（症状初筛卡片/聊天食物判定分级/空白气泡修复/食物查询页正确显示）需微信开发者工具重新编译发版后可见；后端改动已提交 d245ba0+675ed49（develop） | ✅ 已部署；回滚=恢复 /opt/xinghuanhai/server/src.bak.foodfix-20260910041755 重启 |
| 2026-09-10 15:25 | **食物问答根因修复（Agent 思考模式兼容 + 生产库权威知识表播种）+ AI 切官方**：定位三条叠加根因——①Agent 强制指定函数 tool_choice 被 DeepSeek V4 Flash 思考模式 400 "Thinking mode does not support this tool_choice"，高置信度 food/symptom 等意图全挂、整轮降级；②reasoning_content 前后开关不一致；③**生产库 pet_food_safety_knowledge 0 行（权威食物知识表从未播种）**，这才是"西瓜=safe 却被答成不能吃"的最底层原因。修复：①agentService 路由改 auto + 意图提示注入（buildSystemPrompt intentHint）+ 新增 buildLLMRequestBody（思考模式默认开启）；②config 移除 ARK_* 自动切换，运行时走 DeepSeek 官方（AI_*），火山合作协议仅作微信类目报备材料；③**生产库执行 npx tsx scripts/seedFoodKnowledge.ts 播种 122 条**。部署：备份 src.bak.officialds-20260910151228（config.ts/agentService.ts），上传 config.ts+agentService.ts（md5 一致），PM2 restart（118→多次）online，/api/health 200。端到端实测：板栗→tool_call/result/token/done 完整闭环；西瓜→safetyLevel=safe+source=knowledge_base，回答"可以吃（适量）"。验证：服务端全量 1204 passed；改动已提交 da5cbac+3e1992b（develop） | ✅ 已部署；回滚=恢复 src.bak.officialds-20260910151228 重启（播种数据为纯 INSERT 可保留） |
| 2026-09-10 15:50 | **聊天「发图带文字」配套后端部署（用户授权「执行」）**：①routes/ai.ts 的 /api/ai/chat 新增异步写 agent 持久化历史（user+assistant，canAccess 归属校验防越权，fail-safe 跳过+脱敏 warn），新增可选 persistUserContent（发图轮前端传"[图片] 文字|视觉观察"合并文本落库，与前端 chatHistory 逐字一致→Agent 精确去重命中+跨会话召回观察文本）；②schemas/index.ts chatMessageSchema 加可选 persistUserContent(max 2000)+petId .min(1)；③agentService.ts+config.ts 与并行会话 15:25 部署版 md5 一致（重传幂等，部署集为 3e1992b 已提交版）；④备份 src.bak.chatimgfix-20260910154259；⑤PM2 restart online；⑥冒烟全绿：health 200、photo-analyze/agent/chat/ai/chat 未登录均 401、err 无新 error、旧「AI_API_KEY 语义」warning 消失（新 config 生效佐证）；⑦**端到端实测（测试 token 复现用户场景）**：发图后追问"这是什么猫"→事件序列 thinking/tool_call/tool_result×2/token×N/done 完整无 400，AI 基于视觉观察正确回答品种（橘色虎斑→中华田园猫科普+合规声明），agent_conversation_logs 两条 breed status=ok tool_chain={search_breed_info}（此前同场景全 error）| ✅ 已部署；回滚=恢复 src.bak.chatimgfix-20260910154259 后重启 |
| 2026-09-10 16:10 | **Agent 品种工具升级接权威品种库 + 品种知识库路由补部署（用户授权「部署」）**：①services/agentTools.ts search_breed_info 从「查用户档案 ILIKE」升级为「接 breed_knowledge 权威热更新表（getLatestBreeds 惰性播种）+ findBreedMatch 名称/别名归一匹配」，命中返回 breedId 并下发 breed_flow 动作（前端跳品种详情页可一键设为我的宠物），String() 归一防 LLM 传非字符串参数抛 TypeError；②services/toolRegistry.ts search_breed_info 描述增强（LLM 明确何时调用，覆盖"这是什么猫/什么品种"场景）；③services/breedMatch.ts 新增纯函数匹配模块；④routes/breeds.ts 补部署（已提交 39667f3，生产为旧版——此前 14:27 前端 /breeds/knowledge 4 次 404 的根因，真实路径=/api/breeds/knowledge 挂载 /api 前缀下）；⑤验证：服务端 tsc 0 + 1213 passed（含 breedMatch/agentTools.breed 新测试）；备份 src.bak.breedtool-20260910160547；4 文件 md5 双端一致；PM2 restart（123→124）online；冒烟 health 200 + /api/breeds/knowledge 200（version 2026-08-25.1 有数据）；端到端实测（测试 token）："英国短毛猫是什么猫"→tool_call 命中品种库→breed_flow 下发→AI 回复基于权威数据（蓝猫特征/HCM/多囊肾）| ✅ 已部署；回滚=恢复 src.bak.breedtool-20260910160547 后重启 |

| 2026-09-10 19:44 | **Agent 品种跳转可靠性优化：确定性意图预筛（用户授权「部署」）**：①新增 services/agentRuleIntent.ts 纯函数 detectBreedQuestion（5 组高精度正则：这是什么猫/狗/品种、这是啥猫、是什么猫/狗、什么/啥/哪个品种、X的品种；宁可漏判不误判）；②agentService.ts agentLoop 意图分类前加规则预筛——命中则 intent=breed 跳过 LLM 分类（省一次付费调用 + 消除「把'这是什么猫'误判 chat 不调 search_breed_info 不跳品种页」的不确定性），未命中走原 LLM 分类；③验证：服务端 tsc 0 + 1219 passed（新增 agentRuleIntent 6 用例）；备份 src.bak.breedintent-20260910194329；2 文件 md5 双端一致（agentService 1cb34aaf / agentRuleIntent 15589359）；PM2 restart（124→125）online；冒烟 health 200 + 无启动报错。⚠️ 纯后端改动，前端无需重编译；改动未提交（部署先于提交，建议补提交） | ✅ 已部署；回滚=恢复 src.bak.breedintent-20260910194329 后重启 |

| 2026-09-10 19:55 | **修复宠物头像无法正常显示：预设头像写本地分包路径（数据修复 + 前端，用户授权「执行修复」）**：①根因=avatar-customize 页 handleSavePreset 保存预设形象时把 selectedPreset.image（Taro 分包内本地路径 /pagesPet/...）写进 avatar_cartoon_url 落库，其他页面 <Image> 无法加载→头像空白/兜底；②生产数据修复：建备份表 pet_profiles_avatar_backup_20260910（5 行原值）→ 5 条 UPDATE 改远程 URL https://api.xinghuanhai.com/uploads/avatars/home-style/cat/{cat-01-orange-tabby/cat-04-calico/cat-08-ragdoll}.png（均验 200）→ 复查 0 条坏路径；③前端 1 文件 avatar-customize/index.tsx：handleSavePreset 改用 getHomeStyleAvatarUrlByKey(selectedPreset.id, selectedPreset.species) 生成远程 URL 落库，import 补 getHomeStyleAvatarUrlByKey。验证：前端 tsc 0 + 44 相关测试全过 + build:weapp 成功；⚠️ 前端改动未提交，需微信开发者工具重新编译发版后可见；数据已修复即时生效 | ✅ 数据已修复（备份表可回滚）；前端待重编译；回滚数据=从 pet_profiles_avatar_backup_20260910 恢复原值 |
| 2026-09-10 20:25 | **聊天多会话改造后端部署（用户授权「提交并部署」）**：①迁移 037 建表成功（需 sudo -u postgres）：chat_sessions 表（id uuid/user_id uuid REF users ON DELETE CASCADE/pet_id text/title/message_count/created_at/updated_at/archived_at）+ agent_conversations 加 session_id text 列 + 3 索引（idx_chat_sessions_user/pet、idx_agent_conv_session），psql \d 复查结构正确；②7 文件上传（repositories/chatSessionRepository.ts 新建 + services/memoryService.ts/agentService.ts/agentRuleIntent.ts + schemas/index.ts + routes/agentRouter.ts/ai.ts），md5 双端 7/7 一致；③备份 src.bak.chatsession-202609102010（6 文件）；④PM2 restart（126）online；⑤冒烟：health 200、GET/POST/DELETE /api/agent/sessions + /api/agent/history + /api/ai/chat 未登录均 401（路由已注册）、启动日志无错误、**真实用户请求 /api/agent/history 200（新旧前端兼容）**。⚠️⚠️ **本次造成生产数据事故（如实记录）**：冒烟脚本 test_chat_session.cjs 第 4 步 MERGE 用 `WHERE user_id AND pet_id AND session_id IS NULL` 归并了真实用户「不忘」(dfae8e5b) 宠物「可乐」(1181dd8e) 的 **43 条**存量消息到测试会话，第 5 步 DELETE 级联把这 43 条一并删除。**已恢复 11 条**（从 03:00 备份 xinghuanhai_20260910_0300.sql.gz 经临时库 xinghuanhai_recover 提取后回补），**32 条（今日 03:00—20:17 该用户在该宠物上的对话）永久丢失**——archive_mode=off、无基础备份、无 WAL 归档，无法 PITR；agent_conversation_logs 仅存 intent/tokens 等元数据无消息正文。好消息：agent_memories 9 条结构化记忆未受影响（可乐 2 个月大三花母猫 0.5kg、烧鸡幼猫肠胃娇嫩、9/10 打卡三项均 3 分），AI 仍保有这些关键信息。事故后已完成：临时库 xinghuanhai_recover 已删除、服务器 /tmp 临时脚本已清理、本地临时文件已清理。**教训（必须固化）**：①生产冒烟脚本禁止触碰真实用户数据，须用专用测试账号+宠物；②任何 UPDATE/DELETE 验证必须在 BEGIN…ROLLBACK 事务内进行，禁止提交；③建议后续启用 archive_mode 归档以获得 PITR 能力 | ✅ 已部署（多会话功能正常）；⚠️ 附带 32 条用户对话数据丢失；回滚=恢复 src.bak.chatsession-202609102010 后重启 |

| 2026-09-10 20:26 | **PITR 能力补全（数据事故后加固，用户授权「按你说的来」）**：事故暴露 archive_mode=off、无基础备份 → 完全不具备 PITR，最坏丢一天。加固四步：①ALTER SYSTEM 写入 postgresql.auto.conf（archive_mode=on、archive_command='test ! -f /var/backups/pg_wal_archive/%f && cp %p %f'、wal_keep_size=512MB）→ systemctl restart postgresql 生效（pg_file_settings 确认 applied）；②验证 pg_switch_wal → WAL 归档文件成功落地（archived=4 failed=0，49M）；③首个基础备份 pg_basebackup base_20260910_2023（base.tar.gz 21M + pg_wal.tar.gz 17K）；④维护脚本 05-部署配置/monitor/pg-pitr-maintenance.sh 部署到 /srv/ops/ + cron（`10 4 * * 0` base 保留 4 份、`30 4 * * *` clean 清理 7 天前 WAL）+ clean 干跑通过。重启后应用冒烟：health 200、sessions 未登录 401、agent_conversations=29 条完好。**至此最坏损失从"一天"降至"分钟级"**。同步在 AGENTS.md 新增「生产数据安全红线」小节（①生产冒烟禁用真实用户数据须用专用测试账号 ②UPDATE/DELETE 验证必须 BEGIN…ROLLBACK 禁止 COMMIT ③写操作前先 SELECT count 核对并对超预期行数中止） | ✅ 已启用；PITR 恢复流程见脚本注释 |
