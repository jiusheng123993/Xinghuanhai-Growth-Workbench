node_modules
dist
dist-electron
.DS_Store
*.local
.superpowers/
.codegraph/
npm-debug.log*
debug.log
test-result*.txt
screenshot-*.png

# 构建产物
release/
release2/
build/
dist/
dist-electron/

# MCP 工具
codebase-memory-mcp/
codebase-memory-mcp-ui/

# 旧版规则文件
rule-*.md

# 临时脚本
scripts/
*.bat
*.ps1

# 支付配置（包含敏感密钥）
config/payment/
*.pem
*.key
*.p12
*.cer

# 环境变量（包含密钥）
.env
.env.local
.env.*.local
.env.production
.env.development

# 测试覆盖率
coverage/

# 临时爬虫脚本
crawl_breeds.py
insert_breeds.py
temp_check.py
missing_*.json
new_breeds.json
new_breeds.ts

# 根目录 package.json（项目代码在子目录）
/package.json
/package-lock.json

# 小程序构建产物 - 需从Git跟踪中移除
03-源代码/小程序/miniapp/dist/

# 临时目录
.temp/
.work-tmp/
graphify-out/

# 构建/部署产物
03-源代码/小程序/miniapp/dist-h5/
网站部署包/
星寰海官网部署包.zip

# 官网部署包实际文件名（原规则误写成"星寰海官网部署包.zip"，导致"星河宠记官网部署包.zip"一直被 git 跟踪）
*官网部署包.zip

# AI 编码看板工具产物（.board/diffs 单个 5–8MB，曾让 git 历史涨到 831MB）
# 2026-09-11 起不再入库；已入库的历史体积需 filter-repo 重写才能回收，另行处置
.board/

# 运行时上传目录（含用户上传文件与已部署的头像静态资源，不入 git）
03-源代码/server/uploads/
